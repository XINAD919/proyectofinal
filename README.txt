hola y bienvenidos,

para iniciar cuando descarguen el proyecto hay que meter los siguientes elementos(LIB,SCRIPTS y pyvenv.cfg) en una carpeta llamada venv(virtualenv) 
como para tener un orden ya que esos archivos son del entorno virtual
